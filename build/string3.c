#include<stdio.h>
#include<string.h>

void main()
{
   // char name[] = "Abhinav";
 //   printf("Length is %d", strlen( name)); // this is for count length

    /* char oldStr[] = "Abhinav Ahir";
    char newStr[] = "Genzcoder69"; 
    strcat( oldStr, newStr);                // this is for copy string
    puts(oldStr);
    */

   // char fir[100] = "Abhinav ";
    //char sec[] = "Ahir";
    //strcat(fir,sec);                      // this is for addition of string
    //puts(fir);

    char one[] = "Apple"; // A = 65;
    char two[] = "Banana"; // B = 66;
    printf("%d", strcmp(two,one));          // this is for compare the value in ascii mode

}